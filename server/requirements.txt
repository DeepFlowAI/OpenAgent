# Core
fastapi
uvicorn[standard]
pydantic-settings
sqlalchemy[asyncio]
asyncpg
psycopg2-binary
alembic
python-multipart
regex

# Auth (optional)
PyJWT
passlib[bcrypt]

# Redis
# Pin to 5.x: redis-py 6.x sends a RESP3 `HELLO` handshake on connect, which
# Aliyun Redis (older engine / proxy mode) rejects with "unknown command HELLO".
# 5.x defaults to RESP2 and never sends HELLO, so it stays compatible.
redis>=5.0,<6.0

# Knowledge Base - Git & Document Parsing
gitpython
pyyaml

# LLM Gateway
litellm==1.86.2

# AI Search - Embedding & Vector
pgvector
httpx

# OSS
oss2

# Observability — OpenTelemetry SDK + OTLP/HTTP exporter
# (Vendor-neutral: works with SigNoz, Honeycomb, Tempo, etc.)
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-http

# Testing
httpx
pytest
pytest-asyncio
